<%= render "shared/model" %>
<% @model.photos.each do |photo| %>
  <% if can? :read, photo %>
    <div>
      <%= image_tag(photo.image.url)%>
      <% if can? :manage, photo %>
        <%= link_to "delete", [photo.model, photo], :method => :delete, :data => {:confirm => "Delete the photo?"}%>
      <% end %>
    </div>
  <% end %>
<% end %>